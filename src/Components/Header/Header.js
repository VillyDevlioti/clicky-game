import React from "react";
import "./Header.css";

const Header = () => (
    <header id="header">
        <h1 id="title">Clicky Game!</h1>
        <p id="description">This is a memory game! If you click twice on the same image, you loose</p>
    </header>
);

export default Header;